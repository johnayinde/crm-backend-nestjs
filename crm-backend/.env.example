# Application
PORT=3000
NODE_ENV=development
ALLOWED_ORIGINS=http://localhost:3000

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=crm
DB_SYNC=true
DB_SSL=false

# JWT Authentication
JWT_SECRET=secret-key
JWT_EXPIRES_IN=1h

# Rate Limiting
THROTTLE_TTL=60
THROTTLE_LIMIT=10

# Django API
DJANGO_API_URL=http://localhost:8000/api
DJANGO_API_KEY=django-api-key
DJANGO_API_TIMEOUT=5000
DJANGO_API_RETRIES=3

# Redis Cache
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_TTL=300

# Security
ENCRYPTION_KEY=encryption-key-here